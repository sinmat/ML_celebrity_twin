version: '3'

services:
  backend:
    build: .
    volumes:
      - './:/app'
    entrypoint:
      - flask
      - run
      - --host=0.0.0.0
    environment:
      FLASK_DEBUG: 1
      FLASK_APP: ./app.py
    ports:
      - '5000:5000'
  frontend:
    build: ../celebrity-twin-frontend
    volumes:
      - '../celebrity-twin-frontend/:/app'
    ports:
      - '3000:3000'